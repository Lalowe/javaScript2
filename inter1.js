(function(text) {

    let letter = text.charAt(0).toUpperCase(); // here is a text which we inicialies
    console.log(letter + text.substring(1))
}("i am a web developer")); // Here you add vale to TEXT paramter